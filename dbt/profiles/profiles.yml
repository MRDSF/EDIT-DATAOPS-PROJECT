tesla_dbt_proj:
  outputs:
    dev:
      dbname: "{{ env_var('POSTGRES_DB') }}"
      host: database
      pass: "{{ env_var('POSTGRES_PASSWORD') }}"
      port: 5432
      schema: dev
      threads: 1
      type: postgres
      user: "{{ env_var('POSTGRES_USER') }}"
  target: dev
